
{
  "name": "ai-portfolio",
  "version": "1.0.0",
  "private": true,
  "dependencies": {
    "@google/genai": "^1.34.0",
    "markdown-it": "^14.1.0",
    "react": "^19.2.3",
    "react-dom": "^19.2.3"
  },
  "devDependencies": {
    "@types/markdown-it": "^14.1.1",
    "@types/react": "^18.3.3",
    "@types/react-dom": "^18.3.0",
    "esbuild": "^0.23.0",
    "typescript": "^5.5.3"
  },
  "scripts": {
    "build": "mkdir -p dist && cp index.html dist/index.html && esbuild index.tsx --bundle --minify --outfile=dist/index.js --jsx=automatic --define:process.env.API_KEY='\"$API_KEY\"'"
  }
}
